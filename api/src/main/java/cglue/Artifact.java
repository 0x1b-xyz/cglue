package cglue;

/**
 * An abstraction over the {@link App} document base. The application will be loaded from this directory or
 * archive - anything from a directory, war file, zip file or (imaginitively) a remote webdav file system.
 *
 * @author jstiefel
 */
public interface Artifact {
}
